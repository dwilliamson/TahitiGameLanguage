#include <stdio.h>

int Function(void)
{
   int x = 0;

   if (x == 1)
   {
      return (0);
   }
}

void main(void)
{
   int x, y, z;
   signed int t;
   float a, b;

   x = a & b;
   x = 0;
   y = x++ + x++;

   t = 2147483647;

   printf("%d\n", t);
}
